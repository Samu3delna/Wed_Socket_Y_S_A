{
  "name": "live-poll-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "prestart": "npm install ws",
    "start": "node server/index.js"
  },
  "dependencies": {
    "ws": "^8.19.0"
  }
}
